function init(self)
	msg.post(".", "acquire_input_focus")
end

function final(self)
	msg.post(".", "release_input_focus")
end

function on_input(self, action_id, action)
	if action.released then
		factory.create("#factory", vmath.vector3(action.screen_x, action.screen_y, 0))
	end
end
